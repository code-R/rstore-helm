apiVersion: v1
kind: Service
metadata:
  name: {{ template "rstore.fullname" . }}
  labels:
    app: {{ template "rstore.name" . }}
    chart: {{ template "rstore.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rstore.name" . }}
    release: {{ .Release.Name }}
